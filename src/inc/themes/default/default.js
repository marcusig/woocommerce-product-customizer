(function($) {
	if ( ! wp || ! wp.hooks ) return;

	// wp.hooks.addFilter( 'PC.fe.validation.summary_placement', 'MKL/PC/Themes/default', function( placement, ctx ) {
	// 	if ( !PC.utils._isMobile() ) return placement;
	// 	const $container = PC.fe.modal.viewer.$el || PC.fe.modal.$el;
	// 	if ( $container.length ) {
	// 		return { method: 'after', $target: $container };
	// 	}
	// 	return placement;
	// } );

})( jQuery );